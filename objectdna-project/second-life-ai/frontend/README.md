# ObjectDNA Frontend

React/Vite client for the ObjectDNA circular reuse workflow.

```bash
npm install
npm run dev
```

Set `VITE_API_URL` to the backend origin. The UI falls back to local demo data when the API is unavailable.
