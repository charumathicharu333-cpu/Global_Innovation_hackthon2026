import { Icon } from './Icons'

export function HistoryPage({ history, onOpen, onClear, navigate }) {
  return <div className="history-page page-section"><div className="container history-container"><div className="history-heading"><div><div className="section-kicker">PRIVATE OBJECT LEDGER</div><h1>Your analysis<br /><em>history.</em></h1><p>Every scan becomes a reminder that an object is more than its end point.</p></div><div className="history-actions"><button className="button button-dark" onClick={() => navigate('/analyze')}>New analysis <Icon name="arrow" size={16} /></button><button className="button button-outline" onClick={onClear} disabled={!history.length}>Clear history</button></div></div>{history.length ? <div className="history-table" role="table" aria-label="Object analysis history"><div className="history-table-head" role="row"><span>Object</span><span>Date</span><span>Condition</span><span>Next life</span><span>Score</span></div>{history.map((record) => <button className="history-row" key={record.id} onClick={() => onOpen(record)}><span className="history-object"><span className="history-object-icon"><Icon name="box" size={17} /></span><strong>{record.object_name}</strong></span><span>{new Date(record.created_at).toLocaleDateString()}</span><span><i className="history-dot" />{record.condition}</span><span className="history-action">{record.recommended_action}</span><b>{record.life_path_score}</b><Icon name="chevron-right" size={16} /></button>)}</div> : <div className="history-empty"><div className="history-empty-icon"><Icon name="history" size={29} /></div><h2>No scans yet.</h2><p>Start with the damaged wooden table demo. ObjectDNA will show every stage from image to action.</p><button className="button button-dark" onClick={() => navigate('/analyze')}>Start demo <Icon name="sparkles" size={16} /></button></div>}<div className="history-note"><Icon name="shield" size={15} /> This prototype stores history locally. It does not ask for personal information.</div></div></div>
}
